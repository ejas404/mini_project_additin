<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">

</head>

<body>
    <header>
        <%- include('../partials/navbar') %>
    </header>
    <div class="dashboard-container">

        <div class="admin-sidebar">
            <ul>
                <li><a href="/admin/dashboard">Dashboard</a></li>
                <li>Orders</li>
                <li><a href="/admin/products">Products</a></li>
                <li><a href="/admin/userlist">Users</a></li>
            </ul>
            <div class="logout-button">
                <a href="/admin/logout">Logout</a>
            </div>
        </div>
        <div class="userlists-div">
            <div class="back-button">
                <a href="/admin/userlists"><i class="bi bi-arrow-left-short"></i></a>
            </div>
            <div class="head-name">
                <h2>
                    <%= locals.user[0].name.toUpperCase() %>
                </h2>
                <p>
                    <%= locals.user[0].email %>
                </p>
            </div>
            <div class="address">
                <h4>Address</h4>
                <% for (let each of locals.user[0].address) { %>
                    <div class="address-box d-block">
                        <p>
                            <%= each.houseName %>, <%= each.streetAddress %>
                        </p>
                        <p>
                            <%= each.city %>, <%= each.state %>
                        </p>
                        <p>postal code : <%= each.postalcode %>
                        </p>
                        <p>mobile : <%= each.mobile %>
                        </p>
                    </div>
                    <% } %>
            </div>
            <div class="coupons my-4 mx-3">
                <h4 class="my-3">Coupons</h4>
                <div class=" row coupon-list">
                    <% for (let each of locals.coupons) { %>
                    <div class="col-sm-4 d-flex">
                        <div class="card px-3 py-4 text-center" style="width: 15rem;">
                            <h5><%= each.couponCode %></h5>
                            <p ><%= each.couponType === 'percent'? `flat ${each.couponValue}% discount on purchase above Rs.2000`:`flat ${each.couponValue} discount on any purchase`;  %></p>
                        </div>
                    </div>
                    <% } %>
                </div>

            </div>

        </div>
    </div>
    <%- include('../partials/footer')%>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz"
            crossorigin="anonymous"></script>
</body>

</html>